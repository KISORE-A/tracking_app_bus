import React, { useState, useEffect } from "react";
import { getAllUsers } from "../services/api"; export default function LeaveManagement() { const [leaves, setLeaves] = useState([]); const [loading, setLoading] = useState(true); useEffect(() =>{ // Mocking leave data since there's no specific API for it yet, // but it's part of the dashboard requirements. const mockLeaves = [ { id: 1, name: "Varun", role: "Driver", date: "2026-03-20", reason: "Medical", status: "Pending" }, { id: 2, name: "Divya", role: "Student", date: "2026-03-21", reason: "Family Event", status: "Approved" }, { id: 3, name: "Kumar", role: "Teacher", date: "2026-03-19", reason: "Personal Work", status: "Rejected" }, ]; setTimeout(() =>{ setLeaves(mockLeaves); setLoading(false); }, 800); }, []); const getStatusColor = (status) =>{ switch (status) { case "Approved": return "#05CD99"; case "Rejected": return "#EE5D50"; default: return "#FFB547"; } }; if (loading) return<div className="card" style={{ minHeight: "500px", display: "flex", alignItems: "center", justifyContent: "center" }}>Loading Leave Requests...</div>; return (<div className="card" style={{ minHeight: "500px", padding: "1.5rem" }}><h3 style={{ color: "#8a5a00", marginBottom: "1.5rem", textAlign: "left" }}>Leave Requests</h3><div className="table-responsive"><table style={{ width: "100%", borderCollapse: "collapse" }}><thead><tr style={{ borderBottom: "1px solid #edf2f7", textAlign: "left" }}><th style={{ padding: "12px", color: "#a3aed0", fontSize: "0.85rem" }}>NAME</th><th style={{ padding: "12px", color: "#a3aed0", fontSize: "0.85rem" }}>ROLE</th><th style={{ padding: "12px", color: "#a3aed0", fontSize: "0.85rem" }}>DATE</th><th style={{ padding: "12px", color: "#a3aed0", fontSize: "0.85rem" }}>REASON</th><th style={{ padding: "12px", color: "#a3aed0", fontSize: "0.85rem" }}>STATUS</th><th style={{ padding: "12px", color: "#a3aed0", fontSize: "0.85rem" }}>ACTIONS</th></tr></thead><tbody>{leaves.map((leave) =>(<tr key={leave.id} style={{ borderBottom: "1px solid #f7fafc" }}><td style={{ padding: "14px 12px", fontWeight: "700", color: "#8a5a00" }}>{leave.name}</td><td style={{ padding: "14px 12px", color: "#707eae" }}>{leave.role}</td><td style={{ padding: "14px 12px", color: "#707eae" }}>{leave.date}</td><td style={{ padding: "14px 12px", color: "#707eae" }}>{leave.reason}</td><td style={{ padding: "14px 12px" }}><span style={{ padding: "4px 12px", borderRadius: "20px", fontSize: "0.8rem", fontWeight: "700", background: `${getStatusColor(leave.status)}20`, color: getStatusColor(leave.status) }}>{leave.status}</span></td><td style={{ padding: "14px 12px" }}><button style={{ background: "none", border: "none", cursor: "pointer", marginRight: "10px" }}></button><button style={{ background: "none", border: "none", cursor: "pointer" }}></button></td></tr>))}</tbody></table></div></div>);
} 