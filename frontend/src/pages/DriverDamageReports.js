import React from "react";
import Navigation from "../components/Navigation";
import { createDamageReport } from "../services/api";
import { useNavigate } from "react-router-dom";
import "./DamageReports.css"; const MAX_IMAGES = 3; export default function DriverDamageReports() { const navigate = useNavigate(); const [submitStatus, setSubmitStatus] = React.useState({ state: "idle", message: "" }); const [form, setForm] = React.useState({ busNo: "", issueTitle: "", description: "", replacementRequired: "", amount: "", images: [] }); const handleFiles = (files) =>{ const readers = Array.from(files).slice(0, MAX_IMAGES).map((file) =>new Promise((resolve) =>{ const reader = new FileReader(); reader.onload = () =>resolve(reader.result); reader.readAsDataURL(file); })); Promise.all(readers).then((images) =>{ setForm((prev) =>({ ...prev, images })); }); }; const removeImageAt = (idx) =>{ setForm((prev) =>({ ...prev, images: prev.images.filter((_, i) =>i !== idx) })); }; const handleSubmit = async (e) =>{ e.preventDefault(); try { setSubmitStatus({ state: "submitting", message: "" }); const result = await createDamageReport({ ...form, amount: Number(form.amount) || 0 }); setForm({ busNo: "", issueTitle: "", description: "", replacementRequired: "", amount: "", images: [] }); setSubmitStatus({ state: "success", message: "Damage report submitted successfully." }); const createdId = result?.report?._id; if (createdId) { setTimeout(() =>navigate(`/driver/damages/${createdId}`), 350); } } catch (err) { setSubmitStatus({ state: "error", message: err?.message || "Failed to submit damage report." }); } }; return (<><Navigation /><div className="damage-page damage-page--driver"><div className="damage-container"><div className="damage-header"><div><h2>️ Driver Damage Reports</h2><p>Report issues with photos and track approval/payment status.</p></div></div><div className="damage-grid"><div className="damage-form card"><div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", gap: 12, flexWrap: "wrap" }}><h3 style={{ margin: 0 }}>Report New Damage</h3><button type="button" className="secondary" onClick={() =>navigate("/driver/damages")}>View Reports</button></div><form onSubmit={handleSubmit} className="damage-form-grid"><div className="form-row"><label>Bus Number</label><input value={form.busNo} onChange={(e) =>setForm({ ...form, busNo: e.target.value })} placeholder="e.g. TN 33 AB 1234" required /></div><div className="form-row"><label>Issue Title</label><input value={form.issueTitle} onChange={(e) =>setForm({ ...form, issueTitle: e.target.value })} placeholder="e.g. Mirror broken" required /></div><div className="form-row form-row-span"><label>Issue Description</label><textarea rows="3" value={form.description} onChange={(e) =>setForm({ ...form, description: e.target.value })} placeholder="Describe what happened and where the damage is." required /></div><div className="form-row"><label>Replacement Required</label><input value={form.replacementRequired} onChange={(e) =>setForm({ ...form, replacementRequired: e.target.value })} placeholder="e.g. Mirror change" /></div><div className="form-row"><label>Estimated Amount (₹)</label><input type="number" min="0" value={form.amount} onChange={(e) =>setForm({ ...form, amount: e.target.value })} placeholder="0" /></div><div className="form-row form-row-span"><label>Upload Photos (max {MAX_IMAGES})</label><input type="file" accept="image/*" multiple onChange={(e) =>handleFiles(e.target.files)} />{form.images?.length >0 && (<div className="damage-image-grid" aria-label="Selected photos">{form.images.map((src, idx) =>(<div className="damage-image-thumb" key={idx}><img src={src} alt={`selected ${idx + 1}`} /><button type="button" className="damage-image-remove" onClick={() =>removeImageAt(idx)} aria-label="Remove photo">×</button></div>))}</div>)}</div>{submitStatus.state !== "idle" && submitStatus.message && (<div className={`damage-form-alert ${submitStatus.state}`}>{submitStatus.message}</div>)}<button className="primary damage-submit" type="submit" disabled={submitStatus.state === "submitting"}>{submitStatus.state === "submitting" ? "Submitting..." : "Submit Report"}</button></form></div></div></div></div></>);
}
