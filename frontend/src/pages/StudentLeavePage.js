import React from "react";
import Navigation from "../components/Navigation";
import { createLeaveRequest, getLeaveRequests } from "../services/api"; export default function StudentLeavePage() { const [leaves, setLeaves] = React.useState([]); const [form, setForm] = React.useState({ reason: "", fromDate: "", toDate: "", fromTime: "", toTime: "" }); const fetchLeaves = async () =>{ try { const data = await getLeaveRequests(); setLeaves(Array.isArray(data) ? data : []); } catch (err) { console.error(err); } }; React.useEffect(() =>{ fetchLeaves(); }, []); const submit = async (e) =>{ e.preventDefault(); try { await createLeaveRequest(form); setForm({ reason: "", fromDate: "", toDate: "", fromTime: "", toTime: "" }); fetchLeaves(); } catch (err) { alert(err.message); } }; return (<><Navigation /><div style={{ background: "#f4f7fe", minHeight: "100vh", padding: "100px 20px 20px 20px" }}><div style={{ maxWidth: "1100px", margin: "0 auto" }}><h2 style={{ color: "#8a5a00" }}>Student Leave Requests</h2><div className="card" style={{ padding: "1.5rem", marginBottom: "20px" }}><h3 style={{ marginTop: 0, color: "#8a5a00" }}>Request Leave</h3><form onSubmit={submit} style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: "12px" }}><input type="date" value={form.fromDate} onChange={(e) =>setForm({ ...form, fromDate: e.target.value })} required /><input type="date" value={form.toDate} onChange={(e) =>setForm({ ...form, toDate: e.target.value })} required /><input type="time" value={form.fromTime} onChange={(e) =>setForm({ ...form, fromTime: e.target.value })} required /><input type="time" value={form.toTime} onChange={(e) =>setForm({ ...form, toTime: e.target.value })} required /><textarea style={{ gridColumn: "1 / -1" }} rows="3" placeholder="Reason" value={form.reason} onChange={(e) =>setForm({ ...form, reason: e.target.value })} required /><button className="primary" type="submit" style={{ gridColumn: "1 / -1", justifySelf: "center", minWidth: "220px" }}>Submit Leave</button></form></div><div className="card" style={{ padding: "1.5rem" }}><h3 style={{ color: "#8a5a00", marginTop: 0 }}>My Leave Requests</h3><table style={{ width: "100%", borderCollapse: "collapse" }}><thead><tr style={{ textAlign: "left" }}><th>From</th><th>To</th><th>Reason</th><th>Status</th></tr></thead><tbody>{leaves.map((l) =>(<tr key={l._id}><td>{new Date(l.fromDate).toLocaleDateString()} {l.fromTime ? ` ${l.fromTime}` : ""}</td><td>{new Date(l.toDate).toLocaleDateString()} {l.toTime ? ` ${l.toTime}` : ""}</td><td>{l.reason}</td><td>{l.status}</td></tr>))}</tbody></table></div></div></div></>);
} 