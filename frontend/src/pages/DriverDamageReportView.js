import React, { useEffect, useMemo, useState } from "react";
import { Link, useNavigate, useParams } from "react-router-dom";
import Navigation from "../components/Navigation";
import { getDamageReport } from "../services/api";
import "./DamageReports.css"; const StatusPill = ({ label, tone }) =>(<span className={`damage-view-pill ${tone || "neutral"}`}>{label}</span>); export default function DriverDamageReportView() { const navigate = useNavigate(); const { id } = useParams(); const [loading, setLoading] = useState(true); const [error, setError] = useState(""); const [report, setReport] = useState(null); useEffect(() =>{ let isMounted = true; const run = async () =>{ setLoading(true); setError(""); try { const data = await getDamageReport(id); if (!isMounted) return; setReport(data); } catch (e) { if (!isMounted) return; setError(e?.message || "Failed to load report"); } finally { if (isMounted) setLoading(false); } }; if (id) run(); return () =>{ isMounted = false; }; }, [id]); const heroImage = report?.images?.[0] || ""; const createdAt = useMemo(() =>(report?.createdAt ? new Date(report.createdAt) : null), [report?.createdAt]); const statusTone = report?.status === "approved" ? "success" : report?.status === "rejected" ? "danger" : "warning"; const paymentTone = report?.paymentStatus === "paid" ? "success" : "warning"; return (<><Navigation /><div className="damage-page damage-page--driver"><div className="damage-container"><div className="damage-header damage-view-header"><div><h2>Damage Report Details</h2><p>Review the submitted report and track admin approval/payment.</p></div><div className="damage-view-actions"><button className="secondary" onClick={() =>navigate(-1)}>Back</button><Link to="/driver/damages" className="secondary damage-view-link">All Reports</Link></div></div>{loading ? (<div className="card" style={{ padding: "1.5rem" }}>Loading report...</div>) : error ? (<div className="card" style={{ padding: "1.5rem" }}><h3 style={{ marginTop: 0, color: "#8a5a00" }}>Unable to load report</h3><p style={{ color: "#e53e3e" }}>{error}</p><Link to="/driver/damages" className="secondary damage-view-link">Go back</Link></div>) : !report ? (<div className="card" style={{ padding: "1.5rem" }}>Report not found.</div>) : (<div className="damage-view-card">{heroImage ? (<div className="damage-view-hero"><img src={heroImage} alt="damage evidence" /><div className="damage-view-badges"><StatusPill label={(report.status || "pending").toUpperCase()} tone={statusTone} /><StatusPill label={`PAYMENT: ${(report.paymentStatus || "pending").toUpperCase()}`} tone={paymentTone} /></div></div>) : (<div className="damage-view-hero damage-view-hero-empty"><div className="damage-view-badges"><StatusPill label={(report.status || "pending").toUpperCase()} tone={statusTone} /><StatusPill label={`PAYMENT: ${(report.paymentStatus || "pending").toUpperCase()}`} tone={paymentTone} /></div><div className="damage-view-empty">No photos uploaded</div></div>)}<div className="damage-view-body"><div className="damage-view-top"><div><div className="damage-view-bus">{report.busNo || "—"}</div><div className="damage-view-meta">Driver: {report.driverName || "Driver"} {createdAt ? ` • ${createdAt.toLocaleString()}` : ""}</div></div><div className="damage-view-metrics"><div className="damage-view-metric"><div className="label">Estimated</div><div className="amount">₹{Number(report.amount || 0).toLocaleString()}</div></div><div className="damage-view-metric"><div className="label">Platform</div><div>{report.paymentMethod || "—"}</div></div></div></div><div className="damage-view-section"><div className="label">Issue</div><div className="damage-view-text">{report.issueTitle}</div></div><div className="damage-view-section"><div className="label">Description</div><div className="damage-view-text">{report.description}</div></div>{report.replacementRequired ? (<div className="damage-highlight" style={{ marginTop: 14 }}><div className="label">Replacement Required</div><div>{report.replacementRequired}</div></div>) : null} {report.adminNote ? (<div className="damage-view-note"><div className="label">Admin Note</div><div>{report.adminNote}</div></div>) : null} {Array.isArray(report.images) && report.images.length >1 ? (<div className="damage-view-gallery" aria-label="All photos">{report.images.slice(1).map((src, idx) =>(<img key={idx} src={src} alt={`evidence ${idx + 2}`} />))}</div>) : null}</div></div>)}</div></div></>);
} 