import React from "react";
import { getUserProfile } from "../services/api"; export default function StudentFees() { const [profile, setProfile] = React.useState(null); const [loading, setLoading] = React.useState(true); React.useEffect(() =>{ const fetchProfile = async () =>{ try { const data = await getUserProfile(); setProfile(data); } catch (err) { console.error("Failed to fetch profile", err); } finally { setLoading(false); } }; fetchProfile(); }, []); if (loading) return<div className="card" style={{ padding: "1.5rem" }}>Loading fee status...</div>; if (!profile) return<div className="card" style={{ padding: "1.5rem" }}>Unable to load fees.</div>; const totalFees = profile.totalFees || 0; const paidFees = profile.paidFees || 0; const due = Math.max(0, totalFees - paidFees); const percent = totalFees >0 ? Math.round((paidFees / totalFees) * 100) : 0; return (<div className="card" style={{ padding: "1.5rem" }}><h3 style={{ color: "#8a5a00", marginTop: 0 }}>Fee Status</h3><div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(160px, 1fr))", gap: "12px" }}><div style={{ background: "#f8fafc", padding: "12px", borderRadius: "10px" }}><div style={{ fontSize: "0.75rem", color: "#707eae" }}>Total Fees</div><div style={{ fontSize: "1.2rem", fontWeight: 800, color: "#8a5a00" }}>₹{totalFees.toLocaleString()}</div></div><div style={{ background: "#f0fff4", padding: "12px", borderRadius: "10px" }}><div style={{ fontSize: "0.75rem", color: "#2f855a" }}>Paid</div><div style={{ fontSize: "1.2rem", fontWeight: 800, color: "#2f855a" }}>₹{paidFees.toLocaleString()}</div></div><div style={{ background: "#fff5f5", padding: "12px", borderRadius: "10px" }}><div style={{ fontSize: "0.75rem", color: "#e53e3e" }}>Due</div><div style={{ fontSize: "1.2rem", fontWeight: 800, color: "#e53e3e" }}>₹{due.toLocaleString()}</div></div></div><div style={{ marginTop: "14px" }}><div style={{ fontSize: "0.8rem", color: "#707eae", marginBottom: "6px" }}>Payment Progress</div><div style={{ height: "8px", background: "#edf2f7", borderRadius: "999px" }}><div style={{ height: "100%", width: `${percent}%`, background: "linear-gradient(90deg,#d89a10,#05cd99)", borderRadius: "999px" }} /></div><div style={{ marginTop: "6px", fontSize: "0.75rem", color: "#64748b" }}>{percent}% paid</div></div></div>);
} 